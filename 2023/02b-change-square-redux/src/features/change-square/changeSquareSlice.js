import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    bgColor: '#FFFFFF'
}

export const changeSquareSlice = createSlice({
    name: 'changeSquare',
    initialState,
    reducers: {
        
    }
})